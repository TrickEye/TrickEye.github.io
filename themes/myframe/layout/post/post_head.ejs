<div class="post-head">
    <div class="post-info">
        <div class="tag-list">
            <% if (page.tags && page.tags.data && page.tags.data.length > 0) {%>
                <% page.tags.data.forEach(function(tag) { %>
                    <span class="post-tag">
                        <a href="<%-url_for(tag.path)%>">
                            <%=tag.name%>
                        </a>
                    </span>    
                <% }) %>           
            <% } %>
        </div>
        <div class="post-title">
            
            <% if (page.title) {%>
                <%= page.title %>
            <% } else {%>
                *Untitled
            <% } %>
            
        </div>
        <span class="post-date">
            <%- date(page.date, theme.date_format) %>
        </span>
        <div class="h-line-primary"></div>

        <div class="post-toc">

            <details open>
            <summary>目录</summary>
            <%- toc(page.content, {list_number: false}) %>
            </details>
        </div>
    </div>
    <div class="post-img">
        <% if (page.featured_image && page.display_featured_image != false) {%>
            <img src="<%= url_for(page.featured_image)%>" alt="featured_image">
        <% } else { %>
            <div class="h-line-primary"></div>
        <% } %>      
    </div>
</div>